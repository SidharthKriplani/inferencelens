# InferenceLens — dependencies
# Core
pydantic>=2.0.0
python-dotenv>=1.0.0
numpy>=1.24.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0              # for FastAPI test client

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: real model calls (not required for synthetic/demo mode)
# openai>=1.0.0
# anthropic>=0.25.0
